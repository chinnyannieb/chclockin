To build the app, do

mvn clean install

in the project root. This creates chclockin.war in the target directory.

To run the app in-place (does not require Tomcat), do

mvn jetty:run

in the project root. This stands the app up on port 8080